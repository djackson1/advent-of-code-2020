const chai = require("chai");
const { expect } = chai;
// const { ? } = require("./main");

describe("day 1", () => {
  describe("part a examples", () => {
  })

  describe("part b examples", () => {
  });
});
